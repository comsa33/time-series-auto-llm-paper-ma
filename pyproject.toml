[project]
name = "time-series-auto-llm-paper-ma"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.8"
dependencies = [
    "pandoc>=2.4",
    "pillow>=11.2.1",
    "pyyaml>=6.0.2",
]
